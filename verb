#!/usr/bin/env ruby

require_relative 'japanese'

verbs = Japanese.verbs
matches = verbs.select { |verb| verb.hiragana == ARGV[0] }

matches.each do |verb|
  puts "is_u? #{verb.is_u?}"
  puts "is_ru? #{verb.is_ru?}"
  puts "infinitive: #{verb.infinitive}"
  puts "te: #{verb.te}"
  puts "positive present: #{verb.present(true)}"
  puts "negative present: #{verb.present(false)}"
  puts "positive past: #{verb.past(true)}"
  puts "negative past: #{verb.past(false)}"
  puts "positive short: #{verb.short(true)}"
  puts "negative short: #{verb.short(false)}"
  puts
end
